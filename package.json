{
  "name": "@biggerstar/layout",
  "version": "0.0.25-Beta",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build",
    "preview": "vite preview",
    "release": "npm run -s build && npm publish && git commit -am $npm_package_version && git tag $npm_package_version && git push --tags "
  },
  "main": "./dist/any-grid-layout",
  "module": "./dist/any-grid-layout",
  "exports": {
    ".": {
      "default": "./dist/any-grid-layout",
      "import": "./dist/any-grid-layout",
      "require": "./dist/any-grid-layout.umd.cjs"
    }
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.0",
    "rimraf": "^3.0.2",
    "vite": "^3.1.0",
    "vue": "^3.2.45"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/biggerstar/any-grid-layout"
  },
  "author": "biggerstar",
  "license": "GPL-3.0-or-later",
  "description": "",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
  },
  "eslintIgnore": [
    "dist"
  ]
}

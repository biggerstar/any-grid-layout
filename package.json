{
  "name": "@biggerstar/layout",
  "version": "0.0.31-Beta",
  "type": "module",
  "scripts": {
    "dev": "vite-run dev",
    "build": "rimraf dist && vite-run build && vite-run types",
    "size": "vite-run size",
    "preview": "vite-run preview",
    "release": "npm run -s build && git commit -am $npm_package_version && git tag $npm_package_version && git push --tags ",
    "clean": "npm run clean:dist && npm run clean:module",
    "clean:module": "pnpm recursive exec -- rimraf node_modules && rimraf node_modules",
    "clean:dist": "pnpm recursive exec -- rimraf dist && rimraf dist",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:serve": "vitepress serve docs"
  },
  "dependencies": {
    "fast-deep-equal": "^3.1.3",
    "is-what": "^4.1.15",
    "mitt": "^3.0.1",
    "resize-observer-polyfill": "^1.5.1"
  },
  "devDependencies": {
    "@biggerstar/layout": "workspace:0.0.31-Beta",
    "@types/lodash": "^4.14.198",
    "@types/lodash.clonedeep": "^4.5.7",
    "@types/node": "^20.6.1",
    "@vitejs/plugin-vue": "^4.3.4",
    "deepmerge": "^4.3.1",
    "lodash.clonedeep": "^4.5.0",
    "pinia": "^2.1.6",
    "rimraf": "^5.0.1",
    "rollup-plugin-bundle-analyzer": "^1.6.6",
    "typescript": "^5.2.2",
    "vite": "^4.4.9",
    "vite-plugin-copy-dts": "^0.0.5",
    "vite-plugin-dts": "^3.5.3",
    "vite-run": "^1.2.7",
    "vitepress": "1.0.0-rc.10",
    "vue": "^3.3.4"
  },
  "author": "biggerstar",
  "license": "GPL-3.0-or-later",
  "description": "",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "eslintIgnore": [
    "dist"
  ]
}
